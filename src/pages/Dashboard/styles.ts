import styled from 'styled-components';

export const List = styled.div`
  color: #fff;
  position: absolute;

  right: 0;
  top: 0;
  margin-right: 50px;
  margin-top: 50px;

  span {
    &:hover {
      color: #ff9000;
      cursor: pointer;
    }
  }
`;
