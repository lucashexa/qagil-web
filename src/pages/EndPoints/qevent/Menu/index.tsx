import React from 'react';
import Button from '../../../../components/Button';

import { Container } from './styles';

const CreateEvent: React.FC = () => {
  const nk = () => {};

  return (
    <Container>
      <Button>Comece adicionar itens ao menu!</Button>
    </Container>
  );
};

export default CreateEvent;
